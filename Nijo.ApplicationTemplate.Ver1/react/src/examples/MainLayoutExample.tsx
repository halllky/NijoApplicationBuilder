// import * as Layout from '../layout' // 一時的にコメントアウト

export function MainLayoutExample() {
    return (
        // <Layout.MainLayout> // 一時的にコメントアウト
        <div> {/* 代わりに div を使用 */}
            <h1>メインレイアウト (MainLayout)</h1>
            <p>これがメインレイアウトのコンテンツ部分です。</p>
            <p>通常、このコンポーネントはアプリケーション全体のルートで使用されます。</p>
        </div>
        // </Layout.MainLayout> // 一時的にコメントアウト
    )
}
