import * as React from "react"
import * as ReactHookForm from "react-hook-form"
import { QueryModelType } from "../__autoGenerated"
import { MetadataSchema } from "./getSchema"

/** Form内部でのみ使用するコンテキスト */
export type ReflectionFormContext = {
  rootAggregate: QueryModelType
  mode: "single-view" | "search-condition"
  schema: MetadataSchema
  formMethods: ReactHookForm.UseFormReturn<ReactHookForm.FieldValues>
}

/** Form内部でのみ使用するコンテキスト */
export const ReflectionFormContext = React.createContext<ReflectionFormContext>({} as ReflectionFormContext)
