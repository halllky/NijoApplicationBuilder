import * as React from "react"
import * as Input from "../../input"
import * as Layout from "../../layout"
import { VForm3 } from "../../layout"
import { 仕入先マスタDisplayData } from "../../__autoGenerated/仕入先マスタ"
import { useNavigate } from "react-router-dom"

export const 仕入先マスタ一覧検索画面 = () => {
    const navigate = useNavigate()

    /** 一覧表示のカラム定義 */
    const getColumnDefs: Layout.GetColumnDefsFunction<仕入先マスタDisplayData> = React.useCallback(cellType => [
        cellType.text("values.仕入先ID", "仕入先ID"),
        cellType.text("values.仕入先名", "仕入先名"),
        cellType.text("values.担当者名", "担当者名"),
        cellType.tel("values.電話番号", "電話番号"),
        cellType.email("values.メールアドレス", "メールアドレス"),
    ], [])

    return (
        <Layout.MultiView
            queryModel="仕入先マスタ"
            isReady={true}
            title="仕入先マスタ一覧検索"
            getColumnDefs={getColumnDefs}
            headerButtons={
                <button
                    onClick={() => navigate('/仕入先マスタ/new')}
                    className="px-3 py-1 bg-green-500 text-white rounded hover:bg-green-600"
                >
                    新規登録
                </button>
            }
        >
            {/* 検索条件欄 */}
            {({ reactHookFormMethods: { control } }) => (
                <VForm3.Root labelWidth="8rem">
                    <VForm3.BreakPoint>
                        <VForm3.Item label="仕入先ID">
                            <Input.Word name="filter.仕入先ID" control={control} />
                        </VForm3.Item>
                        <VForm3.Item label="仕入先名">
                            <Input.Word name="filter.仕入先名" control={control} />
                        </VForm3.Item>
                        <VForm3.Item label="担当者名">
                            <Input.Word name="filter.担当者名" control={control} />
                        </VForm3.Item>
                    </VForm3.BreakPoint>
                    <VForm3.BreakPoint>
                        <VForm3.Item label="電話番号">
                            <Input.Word name="filter.電話番号" control={control} />
                        </VForm3.Item>
                        <VForm3.Item label="メールアドレス">
                            <Input.Word name="filter.メールアドレス" control={control} />
                        </VForm3.Item>
                    </VForm3.BreakPoint>
                </VForm3.Root>
            )}
        </Layout.MultiView>
    )
}
