import * as React from "react"
import * as ReactHookForm from "react-hook-form"
import * as Input from "../../input"
import * as Layout from "../../layout"
import * as AutoGenerated from "../../__autoGenerated/従業員"

/**
 * 従業員データ1件の詳細を閲覧・編集する画面。
 * URLから従業員IDを取得し、その従業員のデータを取得して表示する。
 */
export const 従業員詳細編集 = () => {

  const { control, handleSubmit } = ReactHookForm.useForm<AutoGenerated.従業員DisplayData>()

  return (
    <Layout.PageFrame
      headerContent={(
        <>
          <Input.IconButton fill>
            保存
          </Input.IconButton>
        </>
      )}
    >
      <Layout.VForm3.Root>
        {/* TODO: ここのデータ構造は 従業員DisplayData に従う。子配列は EditableGrid を使用する。 */}
      </Layout.VForm3.Root>
    </Layout.PageFrame>
  )
}
