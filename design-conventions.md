# 設計規約（モジュール分割規約）

このドキュメントは、このリポジトリ（Nijo, Nijo.GuiClient, demo を含むすべて）で
コードを書く際のモジュール分割の規約を定める。
主な読者は実装を担当する AI である。判断に迷ったら、ここに書かれた判断基準を機械的に適用すること。

適用対象は人間または AI が手で書くソースコード。
コード生成器（Nijo）が出力する自動生成コードそのものは対象外だが、
生成器のテンプレートを書く際は「生成されたコードがこの規約を満たすか」を考慮すること。

---

## 0. 最上位原則

### 0-1. 凝集度は高く、結合度は低く

各クラス・モジュールは 1 つの関心事に責任を持ち、他のモジュールの内部事情を知らないこと。

### 0-2. 最小差分主義の禁止

「目先の指示を満たす最小の変更」で作業を終えてはならない。
**機能追加・不具合修正が終わった時点のアプリケーション全体のコードが、最も綺麗な状態になっていなければならない。**
既存の構造に継ぎ足すと歪みが生じる場合は、構造の方を直すこと。

### 0-3. 指示なしでリファクタしてよい範囲

- **今回の変更が触れたクラス・モジュール、およびその変更によって歪みが生じた呼び出し元・呼び出し先**は、
  指示がなくてもリファクタしてよい。むしろ 0-2 を満たすための義務である。
- 今回の変更と無関係な箇所の改善は、勝手に実施せず提案に留めること。
- 規約に従うと大規模な構造変更が必要になる場合は、コードを歪めて誤魔化さず、
  その旨をユーザーに報告して判断を仰ぐこと。

---

## 1. エントリーポイントは「式次第」である

### 1-1. エントリーポイントの定義

| アプリケーション形態 | エントリーポイント |
|---|---|
| コマンドラインアプリ | コマンドのハンドラー |
| Web サーバー | HTTP エンドポイント直下の処理（MVC なら Controller のアクションメソッド） |
| React | コンポーネント本体（レンダー部）と DOM イベントハンドラー |

### 1-2. 原則

**エントリーポイントを読むだけで「何が・どの順で起きるか」がすべて分かること。**
「プログラム」の本来の意味（式次第・次第書き）のイメージである。

- モジュール間の依存関係と処理順は、エントリーポイントが司る。
- モジュール A の内部からモジュール B を呼び、B の内部から C を呼ぶ……という
  「裏で連鎖する呼び出しチェーン」を作ってはならない。
  A → B → C の順で処理が必要なら、エントリーポイントが A, B, C をその順で呼ぶ。

### 1-3. エントリーポイントに書いてよいもの・いけないもの

書いてよい（書くべき）もの:

- 各モジュールの呼び出しと、その順序
- 「どのモジュールをどの順で呼ぶか」を決めるための分岐・ループ
- モジュール間の入出力の受け渡し・詰め替え

書いてはいけないもの:

- ドメイン計算・業務判断・データ変換の実体（→ 名詞クラスのメソッドへ）

この結果としてエントリーポイントのメソッドが長くなることは**許容する**。
処理の流れが一望できることの方が、1 メソッドの短さより価値が高い。
逆に、エントリーポイントを短くするためだけに処理列を無名の下請けメソッド
（`ProcessCore`, `ExecuteInternal` のような名前しか付けられないもの）へ括り出してはならない。

### 1-4. 複数のエントリーポイントが同じフローを必要とする場合

- そのフロー全体に**業務上自然な名前が付く**場合
  （例:「受注を確定する」「スキーマを検証する」）は、
  該当する名詞クラスの 1 メソッドにまとめ、各エントリーポイントから呼んでよい。
- 自然な名前が付かない場合は共有しない。
  `XxxFlow`, `XxxProcess`, `Shared○○` のようなクラスを作ってはならない。
  各エントリーポイントに式次第を書き、共有は個々のステップ（名詞クラスのメソッド）単位で行う。

判定基準: **括り出したメソッドの名前を、実装を知らない業務側の人に言って通じるか。**
通じるなら共有してよい。通じない（実装の都合の名前になる）なら共有しない。

---

## 2. バックエンド（C#）

### 2-1. クラスは名詞、振る舞いはメソッド

`XxxHelper`, `XxxService`, `XxxManager`, `XxxUtil`, `XxxProcessor` のような
「名詞 + 役割語」のクラスを作ってはならない。

判定手順:

1. `XxxHelper` 等の名前を付けたくなったら、その `Xxx` がドメイン上のオブジェクト（名詞）である。
2. `Xxx` をクラス名とし、Helper / Service / Manager が持つはずだった責務を
   `Xxx` のメソッド（動詞）として定義する。
3. `Xxx` クラスが既に存在するなら、新しいクラスを作らずそこにメソッドを追加する。

```csharp
// NG: 役割語クラスの乱立
class OrderValidator { public bool Validate(Order order) { ... } }
class OrderPriceCalculator { public decimal Calculate(Order order) { ... } }

// OK: 名詞クラスに振る舞いを持たせる
class Order {
    public IEnumerable<string> Validate() { ... }
    public decimal CalculatePrice() { ... }
}
```

役割語クラスは「個々のファイルの見通し」は良くなるが、
クラス数が爆発してアプリケーション全体の把握ができなくなる。
**新しいクラスを作る前に、必ず「既存の名詞に振る舞いを追加できないか」を確認すること。**

### 2-2. 依存の渡し方（名詞クラスに適用）

名詞クラス（ドメインオブジェクト）では:

- そのオブジェクトの**同一性・状態を構成するデータ**はコンストラクタで受け取る。
- DB アクセス・ログ・現在時刻・ファイル IO などの**外部への能力**は、
  それを必要とするメソッドの引数で受け取る。コンストラクタに注入しない。

```csharp
class Order {
    public Order(OrderId id, IReadOnlyList<OrderLine> lines) { ... } // 状態はコンストラクタ

    public void Confirm(MyDbContext db, ILogger logger) { ... }       // 能力はメソッド引数
}
```

理由: メソッドのシグネチャだけで「このメソッドが外部の何に依存するか」が分かるようにするため。

DI コンテナへの登録が前提のクラス（Controller、ホスティング関連など）は
この限りではなく、従来どおりコンストラクタ注入を使ってよい。

### 2-3. 隠蔽の単位は「クラスの公開面」

- 複雑なロジックは、複数のクラス・ファイルに散らばらせるのではなく、
  特定の 1 クラスの中に閉じ込めること。
- クラスの外から見える public メンバーは最小限にし、それぞれに意味の明瞭な名前を付けること。
- クラスの**内部**を private メソッドに分割するのは自由である。
  クラス内部が長くなること自体は問題ではない。問題なのは公開面が広がること。

### 2-4. 「読み飛ばせる量」の原則

**メソッド名・引数・戻り値だけを見て「中身を読まなくてよい」と判断できるコードの量が多いほど、良い設計である。**

- メソッド名は、実装を読まなくても効果が正確に予測できる名前にすること。
- 名前から予測できない副作用（隠れた状態変更、外部への書き込み）を持たせてはならない。
- 予測できない副作用を持つなら、それは名前が間違っているか、分割の単位が間違っている。

### 2-5. 汎用ユーティリティの例外

static クラス・拡張メソッドの集まりは、次の基準を**両方**満たす場合に限り許可する:

1. **「本来 .NET（標準ライブラリやフレームワーク）が提供していてもおかしくない汎用処理」の穴埋めであること。**
   （例: `Util.DotnetEx` は .NET が本来担うべき仕事を仕方なくこのプロジェクト内で定義しているもの。これと同レベルならよい）
2. 純粋であること: 状態を持たず、外部依存を持たず、入出力が引数と戻り値で完結すること。

このリポジトリ・このアプリケーション固有の知識（業務ルール、スキーマの意味、画面の都合）を
少しでも含む処理は、この例外の対象外である。名詞クラスのメソッドとして定義すること。

---

## 3. フロントエンド（React）

### 3-1. 手段の優先順位

何かを実現するとき、必ずこの順で検討し、上位の手段で書ける限り下位を使ってはならない:

1. **レンダー中の導出計算** — 既存の props / state から計算できる値は、変数として導出する。
   新しい state を追加しない。
2. **イベントハンドラ** — ユーザー操作に起因する処理は、その操作のハンドラの中で行う。
   state の更新もここで行う。
3. **ref** — レンダリングに影響しない値の保持、DOM への直接アクセス。
4. **effect** — **外部システムとの同期のみ**に使う。
   （サーバー接続の確立、非 React ライブラリとの同期、ブラウザ API の購読など）

`useEffect` の禁止パターン（AI が特に犯しやすいもの）:

```tsx
// NG: 導出可能な値を state + effect で同期している
const [fullName, setFullName] = useState('')
useEffect(() => { setFullName(`${firstName} ${lastName}`) }, [firstName, lastName])

// OK: レンダー中に導出する
const fullName = `${firstName} ${lastName}`
```

```tsx
// NG: ユーザー操作への反応を effect に逃がしている
useEffect(() => { if (submitted) sendRequest(form) }, [submitted])

// OK: イベントハンドラで直接行う
const handleSubmit = () => { sendRequest(form) }
```

props を state にコピーすること、state の変化を別の state に伝搬させる effect も同様に禁止。

### 3-2. コンポーネントの責務

- 大きく複雑なコンポーネントの本体は、**レイアウト（JSX）と、ステート・フック間の配線**だけに留めること。
- コンポーネント本体とイベントハンドラはエントリーポイントであり、1 章の式次第の原則がそのまま適用される。
  「何がどの順で起きるか」はコンポーネントを読めば分かること。
- 複雑な仕事や処理は、**その仕事を的確に表す名前を付けたうえで**、
  その名を冠するカスタムフックまたはサブコンポーネントに切り出すこと。

判定基準: 切り出したフック / サブコンポーネントの名前が、
その仕事の内容を実装を読まずに言い当てられる名前になっているか。
`useLogic`, `useHandlers`, `InnerComponent` のような、名前が仕事を表さない切り出しは禁止。

---

## 4. 作業完了前のセルフチェック

タスクを完了とする前に、以下を確認すること:

- [ ] `Helper` / `Service` / `Manager` / `Util` / `Processor` という名前のクラスを新設していないか。
      していたら 2-1 の手順で名詞クラスに振る舞いを移す。
- [ ] 新設したクラスは、既存の名詞クラスへのメソッド追加で代替できないか。
- [ ] エントリーポイントを読むだけで処理の全体像が分かるか。
      裏で連鎖する呼び出しチェーンを作っていないか。
- [ ] 追加した `useEffect` は、3-1 の 1〜3 で書き換えられないか。
- [ ] 今回の変更が触れた構造は、継ぎ足しの歪みが残っていない最も綺麗な状態か（0-2, 0-3）。
- [ ] 規約に従えない箇所があれば、誤魔化さずユーザーに報告したか。
