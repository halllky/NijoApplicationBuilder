# fly.io デモ環境の方針メモ

ProjectPage（スキーマ編集画面）内に、編集対象プロジェクトの vite dev プロセスを埋め込み、
訪問者にスキーマ定義と生成後画面の編集体験を提供するデモ環境についての検討結果。

このメモは fly.io 環境に関する前提・注意事項・推奨方針のみを扱う。
localhost 側の実装方針（iframe + postMessage ブリッジ、生成コードへの `data-nijo-node` 注入など）は対象外。

---

## 1. 前提

### 1.1 提供するもの

- 訪問者に ProjectPage を公開する。認証なし。
- 訪問者はスキーマ定義および生成後画面を実際に編集できる。
- 編集結果の即時反映を見せたいため、プレビューは vite dev（ホットリロード）で動かす。

### 1.2 環境構成

- fly machines 上の単一インスタンス。**全ユーザーが1環境を共有する。**
- 定期的にリセットして初期状態に戻す。
- プロジェクト単位・ユーザー単位の環境分離は行わない。

この「単一共有インスタンス」という前提により、マルチテナント構成で必要になる
ポートの動的割当・machine のオーケストレーション・プロジェクト単位の隔離は不要になる。
代わりに以下の 4 点が主要な論点となる。

### 1.3 関連する既存実装の状態

| 対象 | 現状 |
|---|---|
| エディタの配信 | `Nijo/WebService/NijoWebServiceBuilder.cs` が単一 HTML を埋め込みリソースとして返す。dev 時 `localhost:5176` / serve 時 `localhost:5001` |
| プレビュー対象 | `demo/101_販売管理システム/client` → `vite --port 5173 --host` |
| 生成アプリの API 呼び出し | `demo/101_販売管理システム/client/src/example/callAspNetCoreApiAsync.ts` が DEV 時 `http://localhost:5290/` 決め打ち、`credentials: 'include'` |
| React / Vite | React 19.2 / Vite 7.3（モノレポ共通の root node_modules から解決） |

---

## 2. 実装上の必須事項：オリジン統一

fly.io 環境では、以下 2 つの理由により**オリジンを統一しない限り成立しない**。

- **混在コンテンツ**: `https://` のエディタから `http://localhost:*` は iframe 読み込みごとブラウザに拒否される。
- **3rd-party cookie**: `callAspNetCoreApiAsync` は `credentials: 'include'` で Cookie を送るが、クロスサイト iframe 内ではブロックされる。

### 2.1 単一オリジンへの振り分け

`https://<app>.fly.dev` に集約し、`NijoWebServiceBuilder` を振り分け役にする。

```
/                → スキーマエディタ HTML（既存）
/api/*           → スキーマ編集 API（既存）
/preview/*       → vite dev (5173) へリバースプロキシ ※WebSocket upgrade 必須
/preview-api/*   → 生成アプリ WebApi (5290) へリバースプロキシ
```

実装は YARP または `HttpForwarder` の手書き。**HMR の WebSocket を Upgrade 中継できること**が要件。

### 2.2 生成テンプレート側の変更

`callAspNetCoreApiAsync.ts` のベース URL を環境変数で差し替え可能にする（最小変更）。

```ts
const ASP_NET_CORE_BASE_URL = import.meta.env.VITE_API_BASE
  ?? (import.meta.env.DEV ? 'http://localhost:5290/' : '/')
```

### 2.3 vite.config.ts 側の設定

```ts
server: {
  allowedHosts: ['<app>.fly.dev'],   // Vite 7 は既定で外部ホスト名を拒否する
  hmr: { protocol: 'wss', clientPort: 443 },
},
base: '/preview/',
```

fly の https ハンドラは wss をそのまま通すため、HMR は上記設定で動作する。

### 2.4 副産物

オリジン統一により iframe が同一オリジンになるため、
埋め込み内とのやり取りは postMessage ブリッジと DOM 直接アクセスのどちらも選べるようになる。
**localhost 側の実装よりも先に着手する価値がある。**

---

## 3. 注意事項

### 3.1 同時編集の衝突（最大の実務リスク）

全員が 1 つの `nijo.xml` と 1 つの vite dev プロセスを共有する。

- A さんが保存→生成すると `__autoGenerated/*` が書き換わり、**B さんの画面も HMR で勝手にリロードされる**。
- 生成が逐次ファイル書き込みだと、中間状態で tsc が壊れ、**Vite のエラーオーバーレイが全員に表示される**。

**対策（上 2 つは必須）**

1. **生成をアトミックにする** — 一時ディレクトリに全生成してから一括差し替え。中間状態を vite に見せない。
2. **生成処理をサーバー側で直列化** — ロックを取り、実行中は SSE 等で全員に「他の人が生成中」を通知。
3. **共有であることを UI で明示** — 「共有サンドボックスです / 現在 N 人が接続中」を常時表示。
   隠すよりも、他人の編集が流れてくるものとして見せたほうがデモとして筋が通る。

保存は楽観ロックのみ入れ、衝突時は「他の人が更新しました」で再読込させれば十分。

### 3.2 セキュリティ姿勢

訪問者が生成後ソースを編集できる時点で、**そのオリジンで任意の JS を配れる = 実質コード実行**である。
さらに Nijo の生成は C# も出力するため、WebApi を `dotnet watch` で回す構成では**サーバー側 RCE 相当**になる。

これは機能要件そのものなので防げない。**「壊されてよい箱」として設計する。**

- **シークレット・API キーをイメージに一切入れない。**
  AI エージェント連携を行う場合、キーは別の小さなプロキシサービス側に置き、レート制限付きで叩かせる。
- **volume を付けない**（3.3 と同じ理由でもある）。
- **専用の fly org に隔離する。** 6PN のプライベートネットワーク経由で同 org の他アプリに到達できてしまうため。見落としやすい。
- 保存・生成 API に**レート制限**をかける（連打で CPU を焼かれる）。
- Vite の `server.fs.deny` を締め、`/@fs` で読まれて困るファイルをイメージ内に置かない。

リセット間隔が短いほど被害時間が縮む。これは 3.3 の判断材料になる。

### 3.3 リセット設計

**volume を付けない。** プロジェクト一式と `node_modules` をイメージに焼けば、
machine restart がそのまま完全リセットになる。コストゼロ、取りこぼしなし。

**ただし日次固定リセットは推奨しない。** 誰かが壊した状態を次の訪問者が最長 24 時間踏み続ける。

推奨する組み合わせ:

- **アイドルリセット（主）** — 最終操作から 10 分程度無操作なら、イメージ内の pristine コピーから復元し vite を再起動。
  訪問者がほぼ常にクリーンな状態から始められる。
- **手動リセットボタン** — ProjectPage に配置。デモ中に自分で押せるのが実用上効く。
- **日次リセット（保険）** — 上記が機能しなかった場合の最終手段として残す。

`auto_stop_machines` は使わない。vite の cold start + `optimizeDeps` で初回 20〜40 秒かかり、
デモの第一印象を損なう。`min_machines_running = 1` 固定とする。

### 3.4 どこまで「生きた」アプリにするか（要判断）

HMR が即時に効くのは **React 層だけ**。スキーマ変更に伴う C# の再生成は HMR の外側になる。

**(a) フル体験** — WebApi を `dotnet watch` で回す。
スキーマ変更のたびに 10〜30 秒の再ビルドが入り、その間 API は 502。
加えて sqlite のスキーマ変更に伴う DB 再作成が必要で、ここが最も重い。

**(b) React 層に絞る** — 画面 tsx の編集は HMR で即時反映。
スキーマ変更は「生成されるコードが変わる様子」をエディタ側で見せるに留め、実行中アプリは固定データで動かす。
軽く、壊れにくい。

「ホットリロードで即時反映」の主眼が**画面のレイアウトや項目の見た目**であれば (b) で目的の 8 割は取れる。
**まず (b) で立ち上げ、動いてから (a) に広げる。**
(a) を最初から狙うと DB マイグレーションと再ビルド待ちの UX 設計で足を取られる。

### 3.5 サイジング

| プロセス | メモリ目安 |
|---|---|
| vite dev（デモ101規模） | 400〜700MB |
| Nijo serve (.NET) | 〜150MB |
| WebApi `dotnet watch`（(a) 採用時） | 250MB + ビルド時スパイク |

- **shared-cpu-2x / 2GB が下限、4GB 推奨。**
- `swap_size_mb = 1024` を入れておくと、生成時のスパイクによる OOM kill を避けやすい。
- 同時訪問者のボトルネックは vite の transform。数人なら快適、数十人の同時初回アクセスで詰まる。
- **起動時に全ルートを一度叩いて transform キャッシュを温めるウォームアップ**を入れると、初回表示が明確に速くなる。

---

## 4. 推奨方針まとめ

| 項目 | 方針 |
|---|---|
| オリジン | `https://<app>.fly.dev` 単一。`/preview/*` を vite dev へ、`/preview-api/*` を WebApi へプロキシ |
| API ベース URL | `VITE_API_BASE` で差し替え可能にする |
| 永続化 | volume なし。プロジェクトと node_modules はイメージに焼く |
| リセット | アイドル 10 分で pristine 復元（主）＋ 手動ボタン ＋ 日次（保険） |
| machine | `min_machines_running = 1`、`auto_stop_machines` 無効 |
| サイズ | shared-cpu-2x / 4GB、swap 1GB |
| 隔離 | デモ専用の fly org。シークレットをイメージに入れない |
| 生成処理 | アトミック差し替え + サーバー側で直列化 + 実行中の全員通知 |
| スコープ | まず React 層のみ（(b)）で立ち上げ、後から WebApi 連動（(a)）へ |

## 5. 着手順

1. **オリジン統一**（`/preview/*` プロキシ + `VITE_API_BASE` 化）
   — localhost でも fly でも同じ経路になり、以降の作業が全て楽になる
2. **生成のアトミック化 + 直列化** — 共有環境における品質の土台
3. iframe + postMessage ブリッジ + `data-nijo-node` 注入（localhost 側の実装）
4. pristine 復元によるアイドルリセット
5. fly デプロイ（volume なし / 専用 org / `min_machines_running = 1`）

4 と 5 は 1〜3 が localhost で動作してからで問題ない。
