import { useMemo } from "react"
import { UnknownObjectViewer } from "../__autoGenerated"


export default function () {

  const stringArray = useMemo(() => [
    'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaa',
    'bbbbbbbbbbbbbbbbbbbbbbbbbbbbbb',
    (a: number, b: number) => a + b + 1,
    null,
    [undefined, undefined, undefined],
    { a: 'aaaa', b: 'bbb' },
    Symbol(),
  ], [])
  const complexData = useMemo(() => {
    return getObject()
  }, [])

  return (
    <div className="p-4" style={{ fontFamily: '"Cascadia Mono", "BIZ UDGothic"' }}>
      <UnknownObjectViewer label="ただのstring" value="あいうえお" />
      <hr className="my-4 border-1 border-color-8" />
      <UnknownObjectViewer label="ただの配列" value={stringArray} />
      <hr className="my-4 border-1 border-color-8" />
      <UnknownObjectViewer label="テストデータ" value={complexData} estimatedLabelWidth="5.5rem" estimatedValueWidth="16rem" />
    </div>
  )
}

const getObject = (depth: number = 0): {} => ({
  string: 'string',
  number: 123456.789,
  bigInt: 9999999999999999999999999999999999999999999999999999999999999999n,
  true: true,
  false: false,
  null: null,
  undefined: undefined,
  obj: depth >= 3 ? {} : getObject(depth + 1),
  array: depth >= 3 ? [] : Array.from({ length: 4 }).map(() => getObject(depth + 1)),
})
