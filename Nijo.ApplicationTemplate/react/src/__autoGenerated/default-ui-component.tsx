// このファイルは自動生成によって上書きされます。

import React from 'react'
import * as Types from './autogenerated-types'

/**
 * 自動生成されたUIコンポーネントにカスタマイズを加えた後の型の一覧。
 * 各画面ではここに登録されたコンポーネントの中から必要なものをピックアップして画面を組み上げていく。
 */
export const UiContext = React.createContext({} as Types.UiContextValue)

export const UiContextProvider = ({ customizer, children }: {
  customizer: Types.UiCustomizer
  children?: React.ReactNode
}) => {
  const uiContextValue = React.useMemo((): Types.UiContextValue => {
    return customizer({
      /** 既定のログイン画面。何もせずログイン後の画面を表示する。 */
      LoginPage: ({ LoggedInContents }) => LoggedInContents,
    })
  }, [customizer])
  return (
    <UiContext.Provider value={uiContextValue}>
      {children}
    </UiContext.Provider>
  )
}
