using System;
using System.IO;

namespace haldoc {
    class Program {
        static void Main(string[] args) {
            Console.WriteLine("コード自動生成 開始");

            var schema = new テストアプリケーション(new System.Collections.Generic.HashSet<object>());

            File.WriteAllText(
                @"../../../../haldoc/AutoGenerated/EFCode.cs",
                new CodeGenerating.EFCodeGenerator(schema).TransformText());

            Console.WriteLine("コード自動生成 終了");
        }
    }
}
