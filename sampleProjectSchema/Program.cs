using System;
using System.IO;

namespace haldoc {
    class Program {
        static void Main(string[] args) {
            Console.WriteLine("コード自動生成 開始");

            var schemaV2 = new Core.ProjectContext(System.Reflection.Assembly.GetExecutingAssembly());

            File.WriteAllText(
                @"../../../../haldoc/AutoGenerated/EFCode.cs",
                new CodeGenerating.EFCodeGenerator(schemaV2).TransformText());

            Console.WriteLine("コード自動生成 終了");
        }
    }
}
