---
description: 
globs: 
alwaysApply: true
---
- ここはwindows環境です。
  - ファイルの絶対パスは `/c:/aaa/bbb/ccc` ではなく `C:\\aaa\\bbb\\ccc` です。
  - あなたがで使えるターミナルは Windows の powershell です。したがって、例えば複数のコマンドを連続で実行する際に `&&` は使えません。
- 参照しようとしているフォルダに `README.md` という名前のファイルがある場合、 `README.md` を参照してください。
- このワークスペース内部の動作確認などのためにMCP `nijo-mcp` を用意しています。これを使うべき状況では積極的にこれを使ってください。
- .NET の実装の補助のために `dotnet-mcp` を用意しています。これを使うべき状況では積極的にこれを使ってください。
- TypeScript の実装の補助のために `typescript-mcp` を用意しています。これを使うべき状況では積極的にこれを使ってください。
- ソースコードを修正するときは [.editorconfig](mdc:.editorconfig) を参考にしてください。
- コンパイルエラーが出ているファイルの先頭に `このファイルは自動生成されました。このファイルの内容を直接書き換えても、次回の自動生成処理で上書きされるのでご注意ください。` という文字があるとき、そのファイル自身ではなく、そのファイルを自動生成する箇所を修正してください。
- このリポジトリは、大きく分けて、ソースコードを自動生成するプロジェクトと、自動生成されたあとのプロジェクトから成ります。頻出する用語の定義を以下に示します。
  - 「スキーマ定義」とは「nijo.xml」という名前のファイルで定義されるデータ構造や各種オプション属性の定義を表します。
  - 「ソースコード自動生成処理」とは、 [Nijo.csproj](mdc:Nijo/Nijo.csproj) プロジェクトおよびそこで実行される一連のソースコード生成処理を表します。
  - 「アプリケーションテンプレート」とは、 [NIJO_APPLICATION_TEMPLATE.sln](mdc:Nijo.ApplicationTemplate.Ver1/NIJO_APPLICATION_TEMPLATE.sln) に属するソースコードを表します。
